# App Store Connect API Key configuration
app_identifier("com.getmaximumfreedomandfitness.getmaximumfitiosapp")
apple_id(ENV["APPLE_ID"])
team_id(ENV["APPLE_TEAM_ID"])

# For App Store Connect API Key authentication (recommended)
# You'll need to set these environment variables:
# - APP_STORE_CONNECT_API_KEY_ID
# - APP_STORE_CONNECT_ISSUER_ID  
# - APP_STORE_CONNECT_API_KEY_CONTENT (base64 encoded .p8 key)

# Alternatively, you can use Apple ID credentials:
# apple_dev_portal_id(ENV["APPLE_ID"])
# itunes_connect_id(ENV["APPLE_ID"])

# For match (certificate management)
# git_url(ENV["MATCH_GIT_URL"]) # Optional: Git repo for storing certificates
